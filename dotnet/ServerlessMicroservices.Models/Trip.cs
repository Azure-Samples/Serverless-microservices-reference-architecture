using System;
using System.Collections.Generic;
using System.Text;

namespace ServerlessMicroservices.Models
{
    public class Trip
    {
        // TODO: Figure out data types for the following:
        // TripOrigin (lat/long?)
        // TripDestination (lat/long?)
        // PassengeruserName (string?)
        // DriverId (Guid nullable)
        // DateCreated (DateTime (UTC))
        // DateModified (DateTime (UTC))
        // TripStart (DateTime (UTC) nullable)
        // TripEnd (DateTime (UTC) nullable)
        // TripCanceled (bool)
        // RatingByPassenger (float? nullable)
        // RatingByDriver (float? nullable)
    }
}
